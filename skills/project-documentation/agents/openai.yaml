interface:
  display_name: "Project Documentation"
  short_description: "Keep project docs and daily records clear"
  default_prompt: "Use $project-documentation to route durable decisions and update today's development record after project changes."
policy:
  allow_implicit_invocation: true
