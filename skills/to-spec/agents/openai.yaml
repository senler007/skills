interface:
  display_name: "To Spec"
  short_description: "Publish a change-oriented feature spec"
  default_prompt: "Use $to-spec to synthesize this discussion into a tracker-ready change Spec."
policy:
  allow_implicit_invocation: false
