# 2026-08-04

## 目标

完成 [Issue #9](https://github.com/senler007/skills/issues/9)：简化项目文档体系，并统一模块文档工作流。

## 实际实现

- 为仓库写入 GitHub Tracker、项目文档路径和 Senler Skills 使用配置。
- 取消独立 Architecture 文档，把 Design、Architecture、Maintenance Map、常用修改方式和稳定验证方法统一到模块文档规范中。
- 明确已有模块文档可在实现过程中同步；创建新的模块文档边界必须先由人确认。
- 将 Spec 精简为 Goal、Change、Scope、Acceptance & Testing 四个必需部分。
- 明确小而完整的 Spec 可以直接实现，只有需要切片、依赖排序或分批交付时才使用 `$to-tickets`。
- Code Review 保留 Standards、Spec、Documentation 三条独立审查轴，但不再强制创建子 Agent。
- 同步更新中英文 README、Skill metadata 和仓库契约测试。
- 验证：35 项 Python contract tests 全部通过；7 个受影响 Skill 均通过 `quick_validate.py`。
